# Commands exercised — `goals/commands` thread

| Command | E2E / tests | Docs / discoverability | Error coverage | Notes |
|---------|-------------|------------------------|----------------|-------|
| `agcli subnet show` (alias `subnet info`) | `e2e_test::test_subnet_detail_queries` (RPC parity); `cli_test` `parse_subnet_show*`, `parse_subnet_info_alias` | `docs/commands/subnet.md`, `docs/tutorials/getting-started.md`, `docs/llm.txt`, `explain` topics | Exit **12** + hint for unknown netuid; owner tip uses `--netuid` | Step 1 — 2026-03-20 |
| `agcli subnet hyperparams` | Same `e2e_test::test_subnet_detail_queries` (`get_subnet_hyperparams`); `cli_test` `parse_subnet_hyperparams*` | `docs/commands/subnet.md` (discoverability, JSON, exit 12), `docs/llm.txt`, `explain` uses `--netuid <N>` | Unknown netuid → bail same as `show` → exit **12**; validation hint lists `hyperparams` | Step 2 — 2026-03-20 |
| `agcli subnet metagraph` | `e2e_test::test_subnet_detail_queries` (`get_neurons_lite` + count vs `subnet show`); `cli_test` `parse_subnet_metagraph*` | `docs/commands/subnet.md`, `docs/llm.txt`, tutorials (`validator-guide`, `subnet-builder`, `local-testing`), `explain` `--netuid <N>` | Unknown netuid → `require_subnet_exists` → same bail as `show` → exit **12**; validation hint lists `metagraph`; missing `--uid` on valid SN → exit **0** | Step 3 — 2026-03-20 |
| `agcli subnet cost` | `e2e_test::test_subnet_detail_queries` (`pin_latest_block` + `get_subnet_info_pinned` burn vs `subnet show`); `cli_test` `parse_subnet_cost*` | `docs/commands/subnet.md` (discoverability, JSON, exit 12), `docs/llm.txt` | Unknown netuid → `require_subnet_exists` → same bail as `show` → exit **12**; validation hint lists `cost` | Step 4 — 2026-03-20 |
| `agcli subnet emissions` | `e2e_test::test_subnet_detail_queries` (pinned `get_neurons_lite_at_block` + `get_dynamic_info_at_block`: neuron emission sum vs `total_emission()`); `cli_test` `parse_subnet_emissions*` | `docs/commands/subnet.md` (discoverability, JSON, exit 12, pinned snapshot), `docs/llm.txt` | Unknown netuid → `require_subnet_exists` → exit **12**; validation hint lists `emissions` | Step 5 — 2026-03-20 |
| `agcli subnet health` | `e2e_test::test_subnet_detail_queries` (pinned `get_neurons_lite_at_block` + `get_subnet_hyperparams_pinned` + `get_block_number_at`: V/M partition, stale/active bounds); `cli_test` `parse_subnet_health*` | `docs/commands/subnet.md` (discoverability, JSON, exit 12, JSON fields, pinned snapshot), `docs/llm.txt` | Unknown netuid → `require_subnet_exists` → exit **12**; validation hint lists `health` | Step 6 — 2026-03-20 |
| `agcli subnet probe` | `e2e_test::test_subnet_detail_queries` (pinned `get_neuron_at_block` per lite uid + axon-ready count; `cli_test` `parse_subnet_probe*`) | `docs/commands/subnet.md` (HTTP probe, JSON/columns, exit 12, empty metagraph JSON, pinned snapshot), `docs/llm.txt` | Unknown netuid → `require_subnet_exists` → exit **12**; validation hint lists `probe`; probe failures per row exit **0** | Step 7 — 2026-03-20 |
| `agcli subnet commits` | `e2e_test::test_subnet_detail_queries` (`get_block_number` + `get_subnet_hyperparams` + `get_reveal_period_epochs` + `get_all_weight_commits` when CR enabled; `cli_test` `parse_subnet_commits*`) | `docs/commands/subnet.md` (discoverability, JSON fields, exit 12 vs CR-off exit 0, latest-head note), `docs/llm.txt`, cross-links from probe/health/emissions/cost | Unknown netuid → `require_subnet_exists` → exit **12**; validation hint lists `commits`; CR disabled → exit **0** | Step 8 — 2026-03-20 |
| `agcli subnet watch` | `e2e_test::test_subnet_detail_queries` (poll RPC bundle: `get_block_number` + `get_subnet_hyperparams` + `get_dynamic_info`; `cli_test` `parse_subnet_watch*`) | `docs/commands/subnet.md` (discoverability, TUI/ANSI, latest-head, exit 12, transient hyperparams warning), `docs/llm.txt`, cross-links on other subnet **Errors** lines | Unknown netuid → `require_subnet_exists` before loop → exit **12**; validation hint lists `watch`; transient `hyperparams` **None** → warn + keep polling | Step 9 — 2026-03-20 |
| `agcli subnet monitor` | `e2e_test::test_subnet_detail_queries` (`get_block_number` + `get_neurons_lite` vs `subnet show` n; `cli_test` `parse_subnet_monitor*`) | `docs/commands/subnet.md` (discoverability, `--json` vs global `--output`, event types, exit 12, latest-head), `docs/llm.txt`, cross-links on probe / watch / health / emissions / cost / commits | Unknown netuid → `require_subnet_exists` before loop → exit **12**; validation hint lists `monitor` | Step 10 — 2026-03-20 |
| `agcli subnet liquidity` | `e2e_test::test_subnet_detail_queries` (`get_dynamic_info` latest head; `cli_test` `parse_subnet_liquidity*`) | `docs/commands/subnet.md` (discoverability, JSON fields, exit 12 with `--netuid`, zero-pool omission, `explain` cross-ref), `docs/llm.txt`, cross-links on probe / watch / monitor / health / emissions / cost / commits | With `--netuid`: unknown SN → `require_subnet_exists` → exit **12**; validation hint lists `liquidity`; no `--netuid` scans all subnets (no per-SN validation exit) | Step 11 — 2026-03-20 |
| `agcli subnet cache-load` / `cache-list` / `cache-diff` / `cache-prune` | `e2e_test::test_subnet_detail_queries` (`list_cached_blocks` + `load_latest`; `cli_test` `parse_subnet_cache_*`) | `docs/commands/subnet.md` (full cache section: paths, JSON, exit 12 vs empty-cache exit 0, cache-diff vs live), `docs/llm.txt`, `explain` subnet-builder Phase 6; cross-links on probe / watch / monitor / health / emissions / cost / commits / liquidity | Unknown SN → `require_subnet_exists` on all four → exit **12**; validation hint lists `cache-load`, `cache-list`, `cache-diff`, `cache-prune`; missing snapshots → message or JSON error / empty list, exit **0** (see subnet.md); bad `cache-diff` cached block → CLI error (not validation **12**) | Step 12 — 2026-03-20 |
| `agcli subnet emission-split` / `mechanism-count` | `e2e_test::test_subnet_detail_queries` (`get_emission_split` + `get_mechanism_count`; `cli_test` `parse_subnet_mechanism_count`, `parse_subnet_emission_split_*`, `parse_subnet_mechanism_count_*` incl. JSON) | `docs/commands/subnet.md` (discoverability, JSON shapes, exit **12** vs default-split exit **0**), `docs/llm.txt`, `explain` Phase 6; cross-links on other subnet **Errors** lines | Unknown SN → `require_subnet_exists` before storage reads → exit **12**; validation hint lists `emission-split`, `mechanism-count`; unset split → human notice or JSON `configured: false`, exit **0** | Step 13 — 2026-03-20 |
| `agcli subnet check-start` | Same `e2e_test::test_subnet_detail_queries` (`is_subnet_active` + `get_neurons_lite` + `get_subnet_hyperparams` tempo parity vs `subnet show`; `cli_test` `parse_subnet_check_start`, `parse_subnet_check_start_json_output`) | `docs/commands/subnet.md` (discoverability, JSON fields, human messages, exit **12**, `subnet start` cross-ref), `docs/llm.txt`, `explain` Phase 6; **Errors** cross-links updated across subnet.md | Unknown SN → `require_subnet_exists` before RPC bundle → exit **12**; validation hint lists `check-start` (and **`subnet start`**, **`subnet snipe`** this step) | Step 14 — 2026-03-20 |
| `agcli subnet start` / `subnet snipe` (preflight) | Code path + docs only this step (no new e2e extrinsic / long-running snipe): `require_subnet_exists` before wallet (`start`) or before watch/register (`snipe`); snipe not-found / InvalidNetuid text aligned with `subnet show` | `docs/commands/subnet.md` — **`start`** and **`snipe`** sections (discoverability, exit **12**, JSON on snipe success); `llm.txt` bullets | Same **`Subnet N not found`** class → exit **12** before wallet/stream; snipe runtime errors still classified per handler | Step 14 — 2026-03-20 (bundled with check-start) |
| `agcli subnet set-param` | `e2e_test::test_subnet_detail_queries` (hyperparams RPC parity for owner “current value” preview + log line); `cli_test` `parse_subnet_set_param*` (incl. JSON list) | `docs/commands/subnet.md` (**`set-param`** section: discoverability, JSON list, exit **12**, `hyperparameters.md` link), `docs/llm.txt`, `explain` Phase 6 line; **Errors** cross-links include **`set-param`** | Unknown SN → **`require_subnet_exists`** before **`--param list`** / confirm / wallet → exit **12**; validation hint lists **`set-param`**; unknown param / missing `--value` → exit **1** | Step 15 — 2026-03-20 |
| `agcli subnet set-symbol` | `e2e_test::test_subnet_detail_queries` (`get_token_symbol` + `subnet_show.symbol` log); `cli_test` `parse_subnet_set_symbol*` (incl. missing flags, empty symbol parse) | `docs/commands/subnet.md` (**`set-symbol`** section: discoverability, exit **12** vs local symbol **1**, read path, source map), `docs/llm.txt`, `explain` Phase 6; **Errors** lines list **`set-symbol`** with other subnet readers | Unknown SN → **`require_subnet_exists`** after **`validate_symbol`**, before wallet → exit **12**; validation hint lists **`set-symbol`**; bad **`--symbol`** → exit **1**; chain errors (**`SymbolAlreadyInUse`**, etc.) after submit | Step 16 — 2026-03-20 |
| `agcli subnet trim` | `e2e_test::test_subnet_detail_queries` (`subnet_show.max_n` log); `cli_test` `parse_subnet_trim*` | `docs/commands/subnet.md` (**`trim`** section: discoverability, exit **12**, confirm / **`--yes`**, read **`max_n`**, source map, `set-param max_allowed_uids` cross-ref), `docs/llm.txt`, `explain` Phase 6; **Errors** peer lists include **`trim`** | Unknown SN → **`require_subnet_exists`** before wallet → exit **12**; validation hint lists **`agcli subnet trim --netuid <N>`**; decline confirm → exit **0**; chain errors (**`NotSubnetOwner`**, **`TrimmingWouldExceedMaxImmunePercentage`**, etc.) after submit | Step 17 — 2026-03-20 |
| `agcli subnet register-neuron` | `e2e_test::test_subnet_detail_queries` (`subnet_show.burn` log); `cli_test` `parse_subnet_register_neuron*` | `docs/commands/subnet.md` (**`register-neuron`** section: discoverability, exit **12**, read **`burn`**, source map); tutorials fixed to **`--netuid`**; `docs/llm.txt`; `explain` Phase 6 + registration topic strings | Unknown SN → **`require_subnet_exists`** before hotkey unlock → exit **12**; validation hint lists **`register-neuron`** and **`subnet pow`**; chain errors after submit per dispatch | Step 18 — 2026-03-20 |
| `agcli subnet pow` | `e2e_test::test_subnet_detail_queries` (`get_block_info_for_pow` + `get_difficulty` log); `cli_test` `parse_subnet_pow*` / thread edge cases | `docs/commands/subnet.md` (**`pow`**: discoverability, **Read path** / e2e, exit **12** / **`validate_threads`** **1** / no-solution **0**); `docs/llm.txt` (single bullet); `explain` registration / Phase 6 | Unknown SN → **`require_subnet_exists`** before hotkey unlock → exit **12**; validation hint lists **`register-neuron`** and **`subnet pow`**; bad **`--threads`** → **1**; no POW solution in budget → **0** | Step 19 — 2026-03-20 |
| `agcli subnet dissolve` | `e2e_test::test_subnet_detail_queries` (`require_subnet_exists` + **`subnet_dissolve`** log); `cli_test` `parse_subnet_dissolve*` | `docs/commands/subnet.md` (**`dissolve`** + **`root-dissolve`** sections: discoverability, e2e anchor, exit **12** / confirm **0**); `docs/llm.txt`; `explain` Phase 6; **`subnet-builder.md`** `--netuid` fix | Unknown SN → **`require_subnet_exists`** before wallet → exit **12**; validation hint lists **`subnet dissolve`** and **`root-dissolve`**; **`classify_subnet_show_not_found`** asserts both; decline confirm → **0**; owner/root chain errors after submit | Step 20 — 2026-03-20 (**`subnet root-dissolve`** shares the same preflight this step) |
| `agcli subnet terminate-lease` | `e2e_test::test_subnet_detail_queries` (**`subnet_terminate_lease`** log after shared **`require_subnet_exists`**); `cli_test` `parse_subnet_terminate_lease*` | `docs/commands/subnet.md` (**`terminate-lease`** section + **Errors** peer lists); `docs/llm.txt`; `explain` Phase 6 | Unknown SN → **`require_subnet_exists`** before wallet → exit **12**; validation hint lists **`terminate-lease`**; **`classify_subnet_show_not_found`** asserts substring | Step 21 — 2026-03-20 |
| `agcli subnet register-leased` | `e2e_test::test_subnet_detail_queries` (**`subnet_register_leased`** + shared **`get_subnet_registration_cost`** with **`subnet_create_cost`**); `cli_test` `parse_subnet_register_leased_*` | `docs/commands/subnet.md` (**`register-leased`** section); `docs/llm.txt`; `explain` subnet-owner block | No **`require_subnet_exists`** (creates new SN like **`subnet register`**); chain/auth/IO errors per dispatch; not in subnet-not-found validation hint list | Step 22 — 2026-03-20 |
| `agcli subnet create-cost` | `e2e_test::test_subnet_detail_queries` (**`subnet_create_cost`** via **`get_subnet_registration_cost`**, one RPC call also logs **`subnet_register_leased`**); `cli_test` `parse_subnet_create_cost`, `parse_subnet_create_cost_json_output` | `docs/commands/subnet.md` (**`create-cost`** section); `docs/llm.txt`; `explain` Phase 2 + Phase 6 | No **`--netuid`** / no exit **12** for unknown SN; RPC/runtime API read failures use normal network/chain classification | Step 23 — 2026-03-20 |
| `agcli subnet register-with-identity` | `e2e_test::test_subnet_detail_queries` (**`subnet_register_with_identity`** via **`get_subnet_identity`** on test netuid); `cli_test` `parse_subnet_register_with_identity_*` | `docs/commands/subnet.md` (**`register-with-identity`**); `docs/llm.txt`; `explain` Phase 2 + Phase 6; cross-ref from **`subnet register`** | No **`require_subnet_exists`** (new subnet). Pre-unlock: **`validate_subnet_name`** / **`validate_github_repo`** / **`validate_url`** → exit **1** like **`identity set-subnet`**. Chain errors same class as **`subnet register`** + **`InvalidIdentity`** | Step 24 — 2026-03-20 |
| `agcli subnet list` | `e2e_test::test_subnet_detail_queries` (**`subnet_list`** via **`list_subnets`**; asserts test netuid in list when **`subnet show`** hit); `cli_test` **`parse_subnet_list`**, **`parse_subnet_list_with_json_output`**, **`parse_subnet_list_at_block`**, **`parse_subnet_list_with_at_block`**, zero/overflow **`--at-block`**, **`parse_subnet_list_at_block_with_global_flags`** | `docs/commands/subnet.md` (**`subnet list`**: discoverability, **`--at-block`**, JSON/CSV, read path, e2e, source map); `docs/llm.txt`; `explain` subnets topic + Phase 6 | **No** exit **12** (no **`--netuid`**). RPC / bad **`--at-block`** / network errors per **`src/error.rs`**; empty chain → empty output, exit **0** | Step 25 — 2026-03-20 |
| `agcli subnet register` | `e2e_test::test_subnet_detail_queries` (**`subnet_register_plain`** — same **`get_subnet_registration_cost`** log as **`subnet_create_cost`**; documents CLI has no pre-submit read); `cli_test` **`parse_subnet_register`**, **`parse_subnet_register_with_global_flags`** | `docs/commands/subnet.md` (**`subnet register`**: discoverability, read path / e2e, errors, source map); `docs/llm.txt`; `explain` Phase 2 + Phase 6 | **No** exit **12**. Wallet / IO per normal classification. **`SubnetLimitReached`** / **`CannotAffordLockCost`** → **`format_dispatch_error`**; decoded **116** text + hint use **`subnet create-cost`** (`src/chain/mod.rs`) | Step 26 — 2026-03-20 |
| `agcli subnet set-mechanism-count` / `subnet set-emission-split` | `e2e_test::test_subnet_detail_queries` (**`subnet_owner_mechanism_writes`** after **`subnet_emission_split`** / **`subnet_mechanism_count`**); `cli_test` **`parse_subnet_set_mechanism_count*`**, **`parse_subnet_set_emission_split*`** | `docs/commands/subnet.md` (dedicated **`###`** sections; **Errors** peer lists + **`docs/llm.txt`** + **`explain`** Phase 6); validation hint in **`src/error.rs`** | Unknown SN → **`require_subnet_exists`** → exit **12** **before** wallet (**`set-mechanism-count`**); **`set-emission-split`**: bad **`--weights`** → **1** before preflight; unknown SN → **12** after local parse. **`set-emission-split`**: decline confirm → **0** | Step 27 — 2026-03-20 |
| `agcli subnet snipe` | `e2e_test::test_subnet_detail_queries` (**`subnet_snipe_preflight`** → **`get_subnet_info`**); **`e2e_test` sections 6b–6g** (`test_snipe_register`, `test_snipe_fast_mode`, `test_snipe_already_registered`, `test_snipe_max_cost_guard`, `test_snipe_max_attempts_guard`, `test_snipe_watch`); `cli_test` **`parse_subnet_snipe_*`** | `docs/commands/subnet.md` (**`### subnet snipe`**: discoverability, read/e2e, flags, JSON success); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists`** → exit **12** before stream/wallet; invalid **`--max-cost`** (negative / non-finite) → **1** before RPC; burn over cap / insufficient balance / registration disabled → bail after preflight; **`classify_subnet_show_not_found`** includes **`snipe`** | Step 28 — 2026-03-20 (supplements Step 14 **`subnet start`** row for snipe preflight docs) |
| `agcli weights set` | `e2e_test` Phase 7 **`test_set_weights`** (**`weights_set_preflight`** → **`get_subnet_hyperparams`**, then **`set_weights`**); `cli_test` **`parse_weights_set*`**, **`parse_weights_set_*_v2`**, dry-run / JSON / file / stdin | `docs/commands/weights.md` (**`### weights set`**: discoverability, read path, e2e, **`--dry-run`** wallet behavior, on-chain errors); **`docs/llm.txt`**; **`explain`** subnet Phase 6 + **`WEIGHTS`** topic | Unknown SN → **`get_subnet_hyperparams`** **`None`** → exit **12** before wallet; validation hint lists **`weights set`**; bad **`--weights`** / netuid **0** → **1** before RPC; chain errors (`CommitRevealEnabled`, rate limit, stake, version key, …) after submit | Step 29 — 2026-03-20 |
| `agcli weights commit` | `e2e_test` Phase 17 **`test_commit_weights`** (**`weights_commit_preflight`** → **`get_subnet_hyperparams`**, then **`commit_weights`**); `cli_test` **`parse_weights_commit*`**, **`parse_weights_commit_basic`**, missing netuid/weights | `docs/commands/weights.md` (**`### weights commit`**: discoverability, read path, e2e, stake vs set, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights commit`**; RPC hyperparams error → warn + continue; chain errors (`CommitRevealDisabled`, `CommittingWeightsTooFast`, `TooManyUnrevealedCommits`, …) after submit | Step 30 — 2026-03-20 |
| `agcli weights reveal` | `e2e_test` Phase 17 **`test_reveal_weights_rejected_without_prior_commit`** (**`weights_reveal_preflight`** → **`get_subnet_hyperparams`**, then **`reveal_weights`**); same phase: **`test_reveal_weights_rejected_when_reveal_too_early`**, hash mismatch, expiry; `cli_test` **`parse_weights_reveal*`**, **`parse_weights_reveal_basic`**, missing salt/weights | `docs/commands/weights.md` (**`### weights reveal`**: discoverability, read path / salt encoding, e2e, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights reveal`**; RPC hyperparams error → warn + continue; chain errors (`NoWeightsCommitFound`, `InvalidRevealCommitHashNotMatch`, `ExpiredWeightCommit`, `RevealTooEarly`, …) after submit | Step 31 — 2026-03-20 |
| `agcli weights commit-reveal` | `e2e_test` Phase 17 **`test_commit_weights_rejected_when_commit_reveal_disabled`** (**`weights_commit_reveal_preflight`** → **`get_subnet_hyperparams`** with CR interval + tempo + rate limit, then **`commit_weights`** rejected); `cli_test` **`parse_weights_commit_reveal*`** (incl. **`parse_weights_commit_reveal_with_version_key`**, **`--wait`**, stdin/file) | `docs/commands/weights.md` (**`### weights commit-reveal`**: discoverability, strict RPC vs commit/reveal, read path, set_weights fallback, wait/JSON, source map); **`docs/llm.txt`**; **`explain`** subnet Phase 6 | Unknown SN → exit **12** before wallet (same text as other weight readers); validation hint lists **`weights commit-reveal`**; hyperparams RPC error → fail before wallet (**not** warn+continue); CR off → **`set_weights`** path after unlock; CR on → commit + block wait + reveal (chain errors as commit/reveal) | Step 32 — 2026-03-20 |
| `agcli weights status` | `e2e_test` Phase 17 **`test_reveal_weights_rejected_without_prior_commit`** (**`weights_status_preflight`** → same **`try_join!`** as CLI: **`get_weight_commits`** + **`get_block_number`** + **`get_subnet_hyperparams`** + **`get_reveal_period_epochs`**); `cli_test` **`parse_weights_status*`** (incl. missing **`--netuid`**) | `docs/commands/weights.md` (**`### weights status`**: discoverability, read path, e2e, **`subnet commits`** cross-ref, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights status`**; preflight hyperparams RPC → warn + continue; post-wallet bundle errors if RPC/storage fails; no dispatch extrinsic | Step 33 — 2026-03-20 |
| `agcli weights commit-timelocked` | `e2e_test` Phase 17 **`test_commit_timelocked_weights_rejected_when_incorrect_commit_reveal_version`** (**`weights_commit_timelocked_preflight`** → **`get_subnet_hyperparams`** + **`get_commit_reveal_weights_version`**, then wrong-version raw **`commit_timelocked_weights`** expecting **111**); `cli_test` **`parse_weights_commit_timelocked*`** (basic, **`--salt`**, missing **`--netuid`** / **`--weights`** / **`--round`**) | `docs/commands/weights.md` (**`### weights commit-timelocked`**: discoverability, read path, salt/round, e2e, **Timelocked Weights** cross-ref, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights commit-timelocked`**; hyperparams RPC → warn + continue; **`get_commit_reveal_weights_version`** failure before submit → usual RPC classification; dispatch **111** on version mismatch | Step 34 — 2026-03-20 |
| `agcli weights set-mechanism` | `e2e_test` Phase 5 **`test_set_mechanism_weights`** (**`weights_set_mechanism_preflight`** → same **`get_subnet_hyperparams`** branches as **`require_subnet_exists_for_weights_cmd`**, then **`set_mechanism_weights`** mech **0**); `cli_test` **`parse_weights_set_mechanism*`** (basic, **`--version-key`**, missing **`--netuid`** / **`--mechanism-id`** / **`--weights`**) | `docs/commands/weights.md` (**`### weights set-mechanism`**: discoverability, read path, **`--dry-run`**, e2e, **Advanced: Mechanism Weights** cross-ref, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights set-mechanism`**; hyperparams RPC → warn + continue; chain errors overlap **`weights set`** where pallet rules match | Step 35 — 2026-03-20 |
| `agcli weights commit-mechanism` | `e2e_test` Phase 5 **`test_commit_mechanism_weights`** (**`weights_commit_mechanism_preflight`** → same hyperparams branches, then **`commit_mechanism_weights`** mech **0** with blake2 hash from same vector shape as set-mechanism); `cli_test` **`parse_weights_commit_mechanism*`** (basic, **`0x`** hash, missing **`--netuid`** / **`--mechanism-id`** / **`--hash`**) | `docs/commands/weights.md` (**`### weights commit-mechanism`**: discoverability, hash rules vs **`weights commit`**, read path, e2e, **Advanced: Mechanism Weights**, source map); **`docs/llm.txt`**; **`explain`** Phase 6 + **`WEIGHTS`** quick path | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights commit-mechanism`**; bad hex / wrong hash length → **1** before RPC; hyperparams RPC → warn + continue; chain errors overlap **`weights commit`** where pallet rules match | Step 36 — 2026-03-20 |
| `agcli weights reveal-mechanism` | `e2e_test` Phase 5 **`test_reveal_mechanism_weights`** (**`weights_reveal_mechanism_preflight`** → same hyperparams branches as **`require_subnet_exists_for_weights_cmd`**, then **`reveal_mechanism_weights`** with same uids/weights/`MECH_CR_SALT_STR` encoding as **`test_commit_mechanism_weights`**); `cli_test` **`parse_weights_reveal_mechanism*`** (basic, **`--version-key`**, missing **`--netuid`** / **`--mechanism-id`** / **`--weights`** / **`--salt`**) | `docs/commands/weights.md` (**`### weights reveal-mechanism`**: discoverability, salt encoding vs **`weights reveal`**, read path, e2e, **Advanced: Mechanism Weights**, source map); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** before wallet; validation hint lists **`weights reveal-mechanism`**; bad **`--weights`** → **1** before RPC; hyperparams RPC → warn + continue; chain errors overlap **`weights reveal`** where pallet rules match | Step 37 — 2026-03-20 |
| `agcli weights show` | `e2e_test` Phase 37 **`test_all_weights`** (**`weights_show_preflight`** → same **`get_subnet_hyperparams`** branches as **`require_subnet_exists_for_weights_cmd`**, then **`get_all_weights`** + non-empty filter + **`get_neurons_lite`** + **`get_weights_for_uid`** for first validator and Alice when applicable); `cli_test` **`parse_weights_show*`** / **`parse_weights_show_*_v2`** | `docs/commands/weights.md` (**`### weights show`**: discoverability, no wallet, read path, e2e, JSON, hotkey-not-found **1**, source map); **`docs/llm.txt`**; **`explain`** weights quick path + Phase 6 cheat line | Unknown SN → **`require_subnet_exists_for_weights_cmd`** → exit **12** (no wallet); validation hint lists **`weights show`**; hyperparams RPC → warn + continue; bad **`--hotkey-address`** / **`--limit`** → **1**; hotkey not on metagraph → **1**; RPC errors on weight reads per normal classification | Step 38 — 2026-03-20 |
| `agcli block latest` | `e2e_test` Phase 20 **`test_block_queries`** (**`block_latest_preflight`** → same RPC order as **`handle_block`** **`BlockCommands::Latest`**: **`get_block_number`**, **`get_block_hash`**, **`try_join!`**(**`get_block_extrinsic_count`**, **`get_block_timestamp`**)); reuses that hash for header / info assertions; `cli_test` **`parse_block_latest*`**, **`parse_block_with_global_flags`**, etc. | **`docs/commands/block.md`** (**`### block latest`**: discoverability via **`--help`** / **`explain archive`**, read path, JSON fields, e2e, errors); **`docs/llm.txt`** (**### Block**); **`explain`** archive topic + Phase 6 line | No subnet validation; RPC → **10** / timeout **15** / generic **1** per **`classify`**; head \> **`u32::MAX`** → bail **1** (theoretical) | Step 39 — 2026-03-20 |
| `agcli block info` | Same **`test_block_queries`** (**`block_info_preflight`**: **`get_block_hash`** → **`try_join!`**(**`get_block_header`**, **`get_block_extrinsic_count`**, **`get_block_timestamp`**); parity asserts vs **`block_latest_preflight`**); `cli_test` **`parse_block_info*`** | **`docs/commands/block.md`** (**`### block info`**: read path, JSON, errors, e2e); **`docs/llm.txt`**; **`explain`** archive + Phase 6 | Read-only RPC like **`block latest`**; clap errors **2** | Step 40 — 2026-03-20 |
| `agcli block range` | Same **`test_block_queries`** (**`block_range_preflight`**: **`try_join_all`**(**`get_block_hash`**) over a 3-block tail, then **`try_join_all`** of per-hash **`try_join!`**(extrinsics, timestamp)); `cli_test` **`parse_block_range*`**, missing **`--from`/`--to`**, same-block span | **`docs/commands/block.md`** (**`### block range`**: read path, validation exit **1**, JSON/table, e2e); **`docs/llm.txt`**; **`explain`** archive + Phase 6 | **`from` \> `to`** or span \> 1000 → bail **1**; RPC classification same as other block readers | Step 40 — 2026-03-20 |
| **`agcli diff`** (**`portfolio`**, **`subnet`**, **`network`**, **`metagraph`**) | Phase 20 **`test_diff_queries`**: **`diff_portfolio_preflight`**, **`diff_subnet_preflight`**, **`diff_network_preflight`**, **`diff_metagraph_preflight`** (RPC bundles match **`handle_diff`**); `cli_test` **`parse_diff_*`** | **`docs/commands/diff.md`** (discoverability, **`--block1`/`--block2`**, read paths, JSON, exit **1**/**2**/**10**/**15**/**12** for subnet-at-block); **`docs/llm.txt`**; **`explain`** diff topic + Phase 6 | Subnet missing at height → **`Subnet N not found at block B`** → **12** + hint lists **`diff subnet`** / **`diff metagraph`**; portfolio address/wallet → **1**/**12**; clap **2**; RPC **10**/**15**/**1** | Step 41 — 2026-03-20 |
| **`agcli subscribe`** (**`blocks`**, **`events`**) | Phase 26 **`test_subscribe_blocks`** + **`test_subscribe_events_preflight`** (`subscribe_finalized` + **`block.events()`**); `cli_test` **`parse_subscribe_*`** incl. extended **`--filter`** | **`docs/commands/subscribe.md`** (discoverability, read paths, full filter table, JSON, Ctrl+C / reconnect); **`docs/llm.txt`**; **`explain`** Phase 6 | Unknown **`--filter`** → **`Invalid event filter`** → **12** + hint; bad **`--account`** → **12**; clap **2**; persistent WS subscription failures → **10**/**15**/**1** | Step 42 — 2026-03-20 |
| **`agcli doctor`** | Phase 20 **`test_doctor_preflight`** after **`test_diff_queries`**: **`get_block_number`**, **`get_total_networks`**, 3× **`get_block_number`** + **`disk_cache::list_keys`** / **`path`** (mirrors **`handle_doctor`** post-connect); `cli_test` **`parse_doctor_*`** | **`docs/commands/doctor.md`** (discoverability, read path, JSON, **exit 0** with per-row OK/FAIL); **`docs/llm.txt`**; **`explain`** Phase 6; **`docs/commands/utils.md`** cross-link (doctor is top-level, not **`utils`**) | **`doctor`** always returns **`Ok(())`** → process exit **0**; parse **`Connection` / `Block height` / `Latency`** in JSON for automation; invalid globals → clap **2** | Step 43 — 2026-03-20 |
| **`agcli balance`** | Phase 20 **`test_balance_preflight`** after **`test_doctor_preflight`**: **`get_balance_ss58`**, **`get_block_number`** → **`get_block_hash`** → **`get_balance_at_block`** at head (mirrors **`Commands::Balance`** one-shot + **`--at-block`**); `cli_test` **`parse_balance_*`** | **`docs/commands/balance.md`** (discoverability, read path, JSON, watch, archive hint); **`docs/llm.txt`** Tier 1 + table; **`explain`** Phase 6 + tip | Invalid **`--threshold`** / **`balance --threshold`** → classify **12** + hint; bad **`--address`** → **12**; RPC/connect **10**/**15**/**1**; watch tolerates poll errors (**0** until Ctrl+C); clap **2** | Step 44 — 2026-03-20 |
| **`agcli transfer`** / **`transfer-all`** / **`transfer-keep-alive`** | Phase 20 **`test_transfer_preflight`** after **`balance_preflight`**: **`validate_ss58`**(Bob) + **`validate_amount`** + **`get_balance_ss58`**(Alice/Bob) mirroring pre-submit path; Phase 2 **`test_transfer`** (Alice→Bob) + Phase 16 **`test_transfer_all`**; `cli_test` **`parse_transfer*`** etc. | **`docs/commands/transfer.md`** (discoverability, three commands, read path, JSON/dry-run, exit table); **`docs/llm.txt`**; **`explain`** Phase 6 + TIPS | Bad **`--dest`** / **`--amount`** → **12** + hint (**`invalid destination`**, **`transfer amount`**); client-side insufficient vs chain **`insufficient`** → **13**; connect **10**; auth **11**; clap **2**; cancel confirm → **0** | Step 45 — 2026-03-20 |
| **`agcli stake list`** | Phase 20 **`test_stake_list_preflight`** after **`transfer_preflight`**: **`validate_ss58`**(Alice, **`stake list --address`**) + **`get_stake_for_coldkey`** + head **`get_block_hash`** + **`get_stake_for_coldkey_at_block`** (mirrors **`StakeCommands::List`**); Phase 5 **`test_stake_queries`** still covers broader stake RPCs | **`docs/commands/stake.md`** — dedicated **`stake list`** section (discoverability, read path, JSON/CSV, **`--at-block`**, archive hint, exit table, e2e); **`docs/llm.txt`** Tier 1 + Stake table; **`explain`** Phase 6 line + TIPS | Invalid **`--address`** → **12** + **`hint`** (**`stake list --address`** → **`stake.md`**); unresolved coldkey / bad **`--at-block`** → **1**; connect **10**; clap **2**; empty list → **0** | Step 46 — 2026-03-20 |
| **`agcli stake add`** | Phase 20 **`test_stake_add_preflight`** after **`stake_list_preflight`**: **`validate_netuid`**(1) + **`validate_amount`**(**`stake amount`**) + **`check_spending_limit`** + **`get_balance_ss58`**(Alice) + **`try_join!(current_alpha_price, sim_swap_tao_for_alpha)`** (mirrors **`StakeCommands::Add`** pre-wallet RPCs / slippage inputs); Phase 8 **`test_add_remove_stake`** extrinsic | **`docs/commands/stake.md`** — **`stake add`** section (discoverability, read path, **`--max-slippage`**, exit table, e2e); **`docs/llm.txt`** Tier 1 + Stake table + detailed bullet; **`explain`** Phase 6 + TIPS | **`stake amount`** / **`invalid netuid`** / **`spending limit exceeded`** → **12** + hints; client **insufficient** balance / **`--max-slippage`** guard (**`maximum allowed`**) → **13**; chain dispatch errors → **13**; clap **2**; connect **10**; auth **11** | Step 47 — 2026-03-20 |
| **`agcli stake remove`** | Phase 20 **`test_stake_remove_preflight`** after **`stake_add_preflight`**: **`validate_netuid`**(1) + **`validate_amount`**(**`unstake amount`**) + **`try_join!(current_alpha_price, sim_swap_alpha_for_tao)`** (mirrors **`StakeCommands::Remove`** slippage **`--max-slippage`** path; no spending limit / balance pre-read); Phase 8 **`test_add_remove_stake`** | **`docs/commands/stake.md`** — **`stake remove`** section (discoverability, read path vs **`stake add`**, exit table, e2e); **`docs/llm.txt`**; **`explain`** Phase 6 + TIPS | **`unstake amount`** / **`invalid netuid`** → **12** + hints; **`slippage`** + **`maximum allowed`** → **13** (shared with add); dispatch **`NotEnoughStakeToWithdraw`** etc. → **13**; clap **2**; connect **10**; auth **11** | Step 48 — 2026-03-20 |
| **`agcli view portfolio`** | Phase 20 **`test_view_portfolio_preflight`** after **`stake_remove_preflight`**: **`validate_ss58`**(Alice, **`portfolio --address`**) + **`pin_latest_block`** → **`try_join!(get_balance_at_hash, get_stake_for_coldkey_at_block, get_all_dynamic_info_at_block)`**; head **`get_block_hash`** + **`try_join!(get_balance_at_block, get_stake_for_coldkey_at_block)`** (mirrors **`--at-block`**); Phase 21 **`test_view_queries`** still covers broader view RPCs | **`docs/commands/view.md`** — dedicated **`view portfolio`** section (discoverability, **`cargo install`** examples, read path vs **`ViewCommands::Portfolio`**, JSON latest vs **`--at-block`**, exit table, e2e); **`docs/llm.txt`** Tier 1; **`explain`** Phase 6 line + TIPS | Invalid **`--address`** → **12** + **`portfolio --address`** hint → **`view.md`**; clap **2**; connect **10**; **`--at-block`** / pruned archive → **1** with usual hints; **15** when timeout applies | Step 49 — 2026-03-20 |
| **`agcli stake move`** | Phase 20 **`test_stake_move_preflight`** after **`stake_remove_preflight`**: **`validate_netuid(1)`**, **`validate_netuid(2)`**, **`validate_amount`**(**`move amount`**), **`check_spending_limit(2, amount)`** — mirrors **`StakeCommands::Move`** pre-wallet order (no balance/slippage RPC) | **`docs/commands/stake.md`** — **`stake move`** section (discoverability, **`cargo install`** examples, read path vs **`stake_cmds.rs`**, exit table, e2e); **`docs/llm.txt`** Tier 1 + Stake table + detailed bullet; **`explain`** Phase 6 line + TIPS | **`move amount`** / **`invalid netuid`** / **`spending limit exceeded`** → **12** + hints; **`from == to`** → **1**; dispatch errors → **13**; clap **2**; connect **10**; auth **11** | Step 50 — 2026-03-20 |

**Next candidate:** e.g. **`stake swap`**; run full **`e2e_local_chain`** in Docker when convenient.
